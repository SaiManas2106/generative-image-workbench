services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      WORKBENCH_MAX_VRAM_GB: "24"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
